<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:ShelltestApp"
        FlyoutBehavior="Disabled"
       Route="home"
       RouteScheme="app"
       x:Class="ShelltestApp.ShellContainer">
    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <!--does not affect -->
                <!--unselected color for android -->
                <Setter Property="Shell.ShellTabBarUnselectedColor" Value="Green" />
                <!--unselected color for ios -->
                <Setter Property="Shell.ShellUnselectedColor" Value="Green" />
                <!--title color ios -->
                <!--title color and tabselected color for android -->
                <Setter Property="Shell.ShellTitleColor" Value="White" />
                <!--tabselected color ios -->
                <Setter Property="Shell.ShellForegroundColor" Value="White" />
                <!--For the header-->
                <Setter Property="Shell.ShellBackgroundColor" Value="#443E43" />
                <!--For the bottom tab android-->
                <Setter Property="Shell.ShellTabBarBackgroundColor" Value="BlueViolet" />
            </Style>
        </ResourceDictionary>
    </Shell.Resources>
    <ShellItem Route="tabs"
               Style="{StaticResource BaseStyle}"
               Title="Bottom Tab Sample">
        <ShellSection Route="homeTab" Icon="Home.png">
            <ShellContent Route="homeContent" x:Name="home">
                <local:Home/>
            </ShellContent>
        </ShellSection>
        <ShellSection Route="details" Icon="category.png">
            <ShellContent Route="detailTicket" x:Name="details">
                <local:Category/>
            </ShellContent>
        </ShellSection>
        <ShellSection Route="Cart" Icon="ticket.png">
            <ShellContent Route="TicketsList" x:Name="tickets">
                <local:CartWithTickets/>
            </ShellContent>
        </ShellSection>
        <ShellSection Route="Profile" Icon="account.png">
            <ShellContent Route="ContentProfile" x:Name="profile">
                <local:AccountPage/>
            </ShellContent>
        </ShellSection>
    </ShellItem>
</Shell>